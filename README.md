# Jarvis Gmail Connector

Servicio privado para autorizar Gmail mediante OAuth 2.0 y enviar correos desde Jarvis.

## Render

- Build Command: `npm install`
- Start Command: `npm start`
- Health Check Path: `/`

Las credenciales se agregan únicamente como variables de entorno en Render. Nunca se guardan en GitHub.
